<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hh.reggie.mapper.ShoppingCartMapper">
        <select id="getDish" parameterType="java.lang.Long">
            select * from shopping_cart
            where dish_id = #{dishId}
        </select>

        <select id="getSetmeal" parameterType="java.lang.Long">
            select * from shopping_cart
            where setmeal_id = #{setmealId}
        </select>

    <update id="updateById" parameterType="com.hh.reggie.entity.ShoppingCart">
        update shopping_cart
        set name = #{name},image = #{image},user_id = #{userId},
            dish_id = #{dishId},setmeal_id = #{setmealId},
            dish_flavor = #{dishFlavor},number = #{number},
            amount = #{amount}
        where id  = #{id}
    </update>

    <insert id="save" parameterType="com.hh.reggie.entity.ShoppingCart">
        insert into shopping_cart(id,name,image,,user_id,dish_id,setmeal_id,dish_flavor,number,amount,create_time)
        values(#{id},#{name},#{image},#{userId},#{dishId},#{setmealId},#{dishFlavor},#{number},#{amount},#{createTime})
    </insert>

    <select id="list" parameterType="java.lang.Long">
        select * from shopping_cart
        where user_id = #{currentId}
    </select>

    <delete id="remove" parameterType="java.lang.Long">
        delete from shopping_cart
        where user_id = #{currentId}
    </delete>

</mapper>
